import env from 'env';

const externalUrls = {
  help: 'https://support.floos.ae',
  privacyPolicy: `${env.websiteDomain}/privacy-policy`,
  findMyEmployer: `${env.websiteDomain}/find-my-employer`,
  luluOutlets: `${env.websiteDomain}/lulu`,
};

export default externalUrls;